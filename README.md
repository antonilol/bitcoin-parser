# Bitcoin blockchain parser and analyzer

### Usage

- Install `npm` dependencies
- Go in `src/` and copy `analyze.sample.ts` to `analyze.ts`
- Edit `analyze.ts` to your needs
- Build with `npm run build`
- Run with `npm run start`
